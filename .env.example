# Environment Variables for Multi-Agent LangGraph Architecture

# Existing variables (already in Vercel)
# BSA_BASE automatically detects environment from URL
# Use https://rc.bluesquareapps.com for release candidate
# Use https://toolkit.bluesquareapps.com for production
BSA_BASE=https://rc.bluesquareapps.com
BSA_CLIENT_ID=your_client_id
BSA_CLIENT_SECRET=your_client_secret
BSA_REDIRECT_URI=https://your-app.vercel.app/auth/callback
SUPABASE_URL=https://fscwwerxbxzbszgdubbo.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
APP_BASE_URL=https://your-app.vercel.app
OPENAI_API_KEY=your_openai_key

# New variables needed for Phase 1
# Database connection for LangGraph checkpointer and store
# Used by PostgresSaver for conversation state persistence
POSTGRES_CONNECTION_STRING=postgresql://postgres.fscwwerxbxzbszgdubbo:[YOUR-PASSWORD]@aws-0-us-west-1.pooler.supabase.com:6543/postgres

# LangSmith tracing (optional but recommended)
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=your_langsmith_api_key
LANGCHAIN_PROJECT=bsa-multiagent
LANGCHAIN_ENDPOINT=https://api.smith.langchain.com
# CRITICAL for Vercel/serverless: ensures traces complete before function terminates
LANGCHAIN_CALLBACKS_BACKGROUND=false

# Mem0 configuration (optional - for auto memory suggestions)
MEM0_API_KEY=your_mem0_key

# Feature flags for gradual rollout
USE_V2_ARCHITECTURE=false  # Set to true to enable V2 coordinator architecture
SAFE_MODE_DEFAULT=true      # Default to requiring approvals for write operations

# Model configuration
LLM_PLANNER=gpt-4o           # Model for planning DAGs
LLM_FINALIZER=gpt-4o-mini    # Model for response generation
LLM_WORKFLOW=gpt-4o          # Model for workflow design